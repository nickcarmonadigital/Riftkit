# Skill Combinations Cheat Sheet

**Power moves**: Combine skills for multiplied effectiveness.

---

## 🚀 Feature Development Combo

### Full Feature Lifecycle

```
idea_to_spec → BUILD → feature_architecture → security_audit → feature_walkthrough
```

| Step | Skill | Output |
|------|-------|--------|
| 1. Spec it | `idea_to_spec` | Structured requirements |
| 2. Build it | (coding) | Working feature |
| 3. Document it | `feature_architecture` | Architecture doc |
| 4. Secure it | `security_audit` | Security verification |
| 5. Record it | `feature_walkthrough` | User-facing doc |

---

## 🐛 Debugging Combo

### Systematic Bug Hunt

```
bug_troubleshoot → code_review → security_audit
```

| Step | Skill | Purpose |
|------|-------|---------|
| 1. Report | `bug_troubleshoot` | Structure the problem |
| 2. Review fix | `code_review` | Verify the solution |
| 3. Check security | `security_audit` | Ensure fix didn't break anything |

---

## 💼 Client Project Combo

### Full Project Lifecycle

```
client_discovery → proposal_generator → website_build → website_launch
```

| Phase | Skill | Deliverable |
|-------|-------|-------------|
| 1. Intake | `client_discovery` | Understanding + qualification |
| 2. Proposal | `proposal_generator` | Quote + timeline |
| 3. Build | `website_build` | Quality website |
| 4. Launch | `website_launch` | Live site + handoff |

---

## 📝 Documentation Combo

### Complete Doc System

```
sop_standards + wi_standards + schema_standards → ssot_structure → ssot_update
```

| Level | Skill | Creates |
|-------|-------|---------|
| Process | `sop_standards` | Standard operating procedures |
| Task | `wi_standards` | Step-by-step work instructions |
| Data | `schema_standards` | Database/data structure docs |
| Index | `ssot_structure` | Single source of truth |
| Maintain | `ssot_update` | Keeps SSoT current |

---

## 🎬 Content Combo

### Full Content Pipeline

```
video_research → content_creation → (record) → content_cascade
```

| Phase | Skill | Output |
|-------|-------|--------|
| 1. Research | `video_research` | Proven hooks + structures |
| 2. Script | `content_creation` | Optimized script |
| 3. Record | (filming) | Raw video |
| 4. Cascade | `content_cascade` | 30 short-form clips |

---

## 🔄 Session Management Combo

### Context Continuity

```
project_context (start) → ai_tool_orchestration → project_context (end)
```

| When | Skill | Purpose |
|------|-------|---------|
| Session start | `project_context` | Load current state |
| Switch AI | `ai_tool_orchestration` | Transfer context cleanly |
| Session end | `project_context` | Save progress |

---

## 🏗️ New Project Combo

### Project Bootstrap

```
new_project → project_context → idea_to_spec → security_audit
```

| Step | Skill | Sets Up |
|------|-------|---------|
| 1. Init | `new_project` | Project structure |
| 2. Context | `project_context` | Living context doc |
| 3. First feature | `idea_to_spec` | Initial feature spec |
| 4. Baseline | `security_audit` | Security foundation |

---

## 💡 Pro Combos

### The "Ship It Safe" Combo

Before any production deployment:

```
1. security_audit (full checklist)
2. code_review (code review)
3. feature_walkthrough (document what's shipping)
4. project_context (update deployment state)
```

### The "Onboard Fast" Combo

When joining/resuming a project:

```
1. Read project_context
2. Read relevant feature_architecture docs
3. Read security_audit reports
4. Start with idea_to_spec for new work
```

### The "Handoff Perfect" Combo

When handing off to client/team:

```
1. feature_walkthrough (what was built)
2. project_context (current state)
3. sop_standards (any processes they need)
4. security_audit (security posture)
```

---

## 📊 Quick Lookup

| I want to... | Use these skills |
|--------------|------------------|
| Start a new feature | `idea_to_spec` → build → `feature_architecture` |
| Fix a complex bug | `bug_troubleshoot` → fix → `code_review` |
| New client project | `client_discovery` → `proposal_generator` → `website_build` |
| Ship to production | `security_audit` → `feature_walkthrough` → `project_context` |
| Create content | `video_research` → `content_creation` → `content_cascade` |
| Set up docs | `sop_standards` → `schema_standards` → `ssot_structure` |

---

*Combine skills. Multiply results.*
